{
  "name": "zad-dev-devops",
  "version": "1.0.0",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "deploy": "sudo ./scripts/deploy.sh"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
